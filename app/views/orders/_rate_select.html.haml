.panel.panel-success#rate_select
  .panel-heading
    = t('app.rates.slct-button')
  .panel-body
    .form-group.row
      .col-md-6
        = f.label :rate
      .col-md-6
        = select(:order, :rate_id, Rate.all.collect {|p| [p.name, p.id ] }, { include_blank: true }, class: 'form-control')
    = f.error_messages_for(:rate_id)